import { motion } from 'framer-motion';
import { FiMail, FiPhone, FiMapPin } from 'react-icons/fi';

const Contact = () => {
  return (
    <section id="contact" className="py-20 bg-white dark:bg-primary-900">
      // ... existing code ...
    </section>
  );
};

export default Contact; 