import { motion } from 'framer-motion';
import ProjectCard from './ProjectCard';

const Projects = () => {
  return (
    <section id="projects" className="py-20 bg-primary-50 dark:bg-primary-800">
      // ... existing code ...
    </section>
  );
};

export default Projects; 